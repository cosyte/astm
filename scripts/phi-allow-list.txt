# scripts/phi-allow-list.txt
#
# PHI-scan allow-list: the positive declaration that a fixture's identifiers
# are synthetic. `scripts/phi-scan.ts` reads this file; any realistic-PHI-shaped
# token NOT covered here is a hit. Byte-strict wire formats cannot carry an
# inline `# synthetic: true` header (the header segment must be the first bytes),
# so this allow-list is the synthetic-fixture declaration mechanism, the same
# approach every sibling `@cosyte/*` parser uses.
#
# Format: `<TAG> <value>` per line. `#` comments and blank lines are ignored.
#   NAME <token>          synthetic person-name word, matched case-insensitively
#   DOB <value>           synthetic date of birth (normalize as your detector does)
#   ID <value>            synthetic id matching an SSN / MRN / member-id shape
#   EMAILDOMAIN <domain>  reserved / test email domain that is allowed to appear
#
# NOTE: the floor consumes EMAILDOMAIN (SSN + email shapes). The ASTM P-record
# detector added to scripts/phi-scan.ts consumes NAME + DOB: the patient name
# tokens (P field 6) and birthdates (P field 8) of every synthetic fixture must
# be declared below, or the scanner flags them. ID entries remain reserved for a
# later phase's practice/lab-ID detector.
#
# To add a new synthetic fixture: reuse the tokens below, or append new ones here
# and commit. Adding a token is a reviewed, intentional act: never silent.

# --- Reserved / test email domains (RFC 2606 / 6761) ---
EMAILDOMAIN example.com
EMAILDOMAIN example.org
EMAILDOMAIN example.net
EMAILDOMAIN example.test

# --- Synthetic person-name tokens (P field 6, Last^First^Middle; P field 7, mother's maiden) ---
# All fully synthetic.
NAME DOE
NAME JANE
NAME Q
NAME ROE
NAME RICHARD
NAME KING

# Inline `.ts` fixtures. These were read by hand until the sweep's roots reached
# `test/**/*.test.ts` and the record detector learned to read a stream written as
# a string literal; every one is a made-up token in a hand-authored stream.
#
# THIS BLOCK IS EXACTLY THE SET THE WIDENED SWEEP REPORTED, no wider. Tokens that
# a sibling repo declares, and tokens this repo's own negative corpus uses to
# prove the detector still fires, are deliberately ABSENT: declaring a token the
# sweep never raised buys nothing and silently retires a case. Re-derive the set
# with `pnpm phi-scan`, never by reading this list.
NAME JOHN
NAME ANNA
NAME GRAZYNA
NAME SYNTHETIC
NAME PATIENT
NAME JAN
NAME AUKASZ
NAME MÜLLER
NAME SMITH
NAME SYNTHA
NAME SYNTHB
NAME ALPHA
NAME BRAVO

# The escape-sequence fixtures put an escape sequence INSIDE the name field, so
# splitting on the component delimiter yields a token that is an artifact of the
# contested reading rather than a name: `DOE^JANE^A` written with a sequence in
# it splits as `DOE&F&`, `&JANE`, and a bare middle initial. The tokens are
# declared as they are read, because a detector that "helpfully" decoded them
# would be guessing which of the two competing alignments is the one to judge,
# and that guess is exactly what this package refuses to make everywhere else.
# A ONE-CHARACTER token is not declared here and never should be: the detector
# does not read one at all, because exempting a bare initial or a bare delimiter
# by name would exempt that character for the whole repository forever.
NAME DOE&F&
NAME DOE&S&\&JANE
NAME &JANE
NAME &Z&JANE
NAME O&BRIEN
NAME O&E&BRIEN
NAME O&BRI
NAME NEJAN
NAME A&Z&B

# --- Synthetic dates of birth (P field 8, YYYYMMDD[HHMMSS]) ---
DOB 20200101
DOB 19850612
# Deliberately truncated (7-digit) synthetic DOB: the partial-timestamp fixture.
DOB 2020010
# Inline `.ts` fixtures, same widening and the same "exactly the reported set"
# rule as the name tokens above.
DOB 19700101
DOB 19800101
DOB 20191231

# --- Synthetic ids: SSN / MRN / member-id shapes (reserved for a later phase's detector) ---
# ID MRN12345
