---
"@cosyte/astm": patch
---

Documentation: the page describing what the library does and does not do moves from an "About"
category into **Troubleshooting**, matching the section order every other `@cosyte/*` package uses.

The page's URL is unchanged, so existing links to it still resolve. It now sits beside the
troubleshooting guide that already pointed at it.
